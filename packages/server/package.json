{
  "name": "@correctr/server",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "docker:build": "docker build -t correctr-server .",
    "docker:run": "docker run --rm -p 3000:3000 --env-file .env correctr-server"
  },
  "dependencies": {
    "effect": "^3.12.0",
    "hono": "^4.7.0",
    "openai": "^6.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
